import React, {Component, Fragment} from 'react';
import './timer.less';

class Timer extends Component {
    render() {
        return (
            <Fragment>
                <h1>倒计时器</h1>
            </Fragment>
        );
    }
}

export default Timer;
